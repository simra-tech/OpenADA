interface:
  display_name: "Characterize Analog Block"
  short_description: "Reason about and characterize analog blocks"
  default_prompt: "Use $openada:characterize-analog-block to resolve the immediate analog behavior, then execute only the next justified semantic intent."
